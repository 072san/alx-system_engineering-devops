

#!/bin/bash
# Set the parent directory path
parent_dir="../"
# Copy HTML files from the current working directory to the parent directory
find . -maxdepth 1 -type f -name "*.html" -newer "$parent_dir" -exec cp -u '{}' "$parent_dir" \;



